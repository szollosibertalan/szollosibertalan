name: Generate Prisma PDF from Markdown

on:
  push:
    paths:
      - 'docs/Prisma-guide.md'
  workflow_dispatch: {}

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install pandoc and LaTeX
        run: |
          sudo apt-get update
          sudo apt-get install -y pandoc texlive-xetex texlive-fonts-recommended

      - name: Convert Markdown to PDF
        run: |
          pandoc docs/Prisma-guide.md -o docs/Prisma-guide.pdf --pdf-engine=pdflatex

      - name: Commit PDF
        uses: EndBug/add-and-commit@v9
        with:
          author_name: 'github-actions'
          author_email: 'actions@users.noreply.github.com'
          message: 'chore: add generated Prisma PDF'
          add: 'docs/Prisma-guide.pdf'
          push: true
          github_token: ${{ secrets.GITHUB_TOKEN }}
